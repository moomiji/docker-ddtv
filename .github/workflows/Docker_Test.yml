name: DDTV_Test

on:
  release:
    types: [released]
  watch: # 合并删除
    types: [started]
  push:
    branches: 
      - test

jobs:
  job1:
    uses: ./.github/workflows/test.yml
    with:
      test: test
