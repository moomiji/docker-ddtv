name: test2

on:
  workflow_call:
    inputs:
      test:
        type: string
      t:
        required: true
        type: string

jobs:
  Build_and_push:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{ inputs.t }}
          echo ${{ inputs.test }}
          echo ${{ inputs.t }}
          echo fin
